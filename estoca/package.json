{
  "name": "estoca",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Estoca — real-time stock control for single-branch retail SMBs (Genesis slice).",
  "scripts": {
    "dev": "vite",
    "dev:api": "tsx watch src/server/http.ts",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^26.1.0",
    "better-sqlite3": "^12.11.1",
    "tsx": "^4.23.0",
    "typescript": "^5.6.3",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
